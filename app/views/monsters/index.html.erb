<div class="species-grid">

  <div class="species-group species-bird">
    <h2>鳥竜種</h2>
    <ul>
      <% @monsters_by_species["鳥竜種"].each do |monster| %>
        <li>
          <%= link_to monster.name, monster_path(monster) %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="species-group species-flying">
    <h2>飛竜種</h2>
    <ul>
      <% @monsters_by_species["飛竜種"].each do |monster| %>
        <li>
          <%= link_to monster.name, monster_path(monster) %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="species-group species-elder">
    <h2>古龍種</h2>
    <ul>
      <% @monsters_by_species["古龍種"].each do |monster| %>
        <li>
          <%= link_to monster.name, monster_path(monster) %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="species-group species-fish">
    <h2>魚竜種</h2>
    <ul>
      <% @monsters_by_species["魚竜種"].each do |monster| %>
        <li>
          <%= link_to monster.name, monster_path(monster) %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="species-group species-fanged">
    <h2>牙獣種</h2>
    <ul>
      <% @monsters_by_species["牙獣種"].each do |monster| %>
        <li>
          <%= link_to monster.name, monster_path(monster) %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="species-group species-crustacean">
    <h2>甲殻種</h2>
    <ul>
      <% @monsters_by_species["甲殻種"].each do |monster| %>
        <li>
          <%= link_to monster.name, monster_path(monster) %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="species-group species-sea">
    <h2>海竜種</h2>
    <ul>
      <% @monsters_by_species["海竜種"].each do |monster| %>
        <li>
          <%= link_to monster.name, monster_path(monster) %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="species-group species-brute">
    <h2>獣竜種</h2>
    <ul>
      <% @monsters_by_species["獣竜種"].each do |monster| %>
        <li>
          <%= link_to monster.name, monster_path(monster) %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="species-group species-unknown">
    <h2>亜龍種</h2>
    <ul>
      <% @monsters_by_species["亜龍種"].each do |monster| %>
        <li>
          <%= link_to monster.name, monster_path(monster) %>
        </li>
      <% end %>
    </ul>
  </div>

</div>
